#ifndef GEARCLASSREGISTRAR_H
#define GEARCLASSREGISTRAR_H

#include <phpcpp.h>
#include <../includes/_gear.h>
#include <headers/gearbaseclass.h>

class GearClassRegistrar// : public GearBaseClass
{
private:
    //std::vector<RegistrarCallback*> _callbacks;

    //static GearClassRegistrar _instance;

public:
//    GearClassRegistrar() = default;

//    void add(RegistrarCallback* callback);
//    void remove(RegistrarCallback* callback);
    //static void executeAll(const Php::Extension &extension);

    //static GearClassRegistrar getInstance();

};

#endif // GEARCLASSREGISTRAR_H
